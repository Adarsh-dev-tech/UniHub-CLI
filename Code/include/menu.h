/*
    menu.h

    This header file declares the main entry point for running the UniHub-CLI application.
    It provides the interface for launching the application's menu system and handling
    user interactions. The functions declared here are implemented elsewhere and are
    responsible for managing the application's workflow and user experience.
*/

#pragma once           // Ensures this header is included only once during compilation

#include <string>      // Includes the standard string library (may be used in implementation)

namespace uni {        // Encapsulates declarations within the 'uni' namespace

void runApp();         // Declares the function to start and run the application

} // namespace uni
